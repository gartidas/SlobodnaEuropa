import DefaultLayout from "../../sharedComponents/molecules/DefaultLayout";
import DetailTemplate from "../../sharedComponents/templates/Detail";

const ArticleDetail = () => {
  return (
    <DefaultLayout>
      <DetailTemplate />
    </DefaultLayout>
  );
};

export default ArticleDetail;
